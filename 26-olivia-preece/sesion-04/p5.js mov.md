# sesión 04 - 10/04

### ALGORITMO  
Secuencia instrucciones paso a paso, lógicas, definidas, ordenadas y finitas que solucionan un problema o realiza una tarea específica.  
CARACTERÍSTICAS FUNDAMENTALES  
* Presición: Cada paso debe estar muy claro
* Orden: Los pasos llevan una secuencia lógica
* Finitud: Tiene que terminar en algún momento; no puede ser finito
* Definición: Si sigues el mismo algoritmo dos veces con los mismos datos, deberías obtener siempre el mismo resultado

### ESTRUCTURA  
* Input (entrada): La información o los ingredientes que necesitas para empezar.  
* Proceso (algrotimo): Los pasos lógicos que transforman esa entrada
* Onput (salida): El resultado final o la solución al problema

### DIAGRAMA DE FLUJO  
Representación gráfica de un algoritmo o de los pasos de un proceso. En programación, se utiliza como una herramienta de planificación para visualizar la lógica de un programa antes de escribir una sola línea de código.  
Se usan componentes Estándar (simbología), para que cualquier programador pueda entenderlo.

### LENGUAJE DE PROGRAMACIÓN  
Existen entre 700 y 900 lenguajes de programación que se utilizan actualmente en la industria, la academia y el desarrollo de software profesional.  
Si incluimos lenguajes antiguos (muchos ya en desuso), dialectos específicos y lenguajes creados para investigación, la cifra supera los 8,000 o incluso 9,000 según repositorios como Online Historical Encyclopedia of Programming Languages

# P5.js  
Lenguaje de programación -> utilza principalmente el lenguaje JavaScript  
### LIBRERÍA DE JAVASCRIPT  
P5.js no es un lenguaje nuevo desde 0, sino que una biblioteca (library) de JavaScript. Esto significa que usa toda la potencia y la sintaxis de JavaScript, pero te regala un "vocabulario" especializado para dibujar, animar y crear cosas visuales de forma mucho más sencilla.

### FUNCIONES MAESTRAS  
* Setup(): Se ejecuta una sola vez al principio (para crear el lienzo). Su propósito es configurar el entorno inicial. Define el tamaño de lienzo (createCanvas), cargas imágenes o sonidos, y establece configuraciones que no cambiarán (como el color de fondo inicial).
* draw(): Se ejecuta en un bucle infinito (normalmente 60 veces por segundo)



### movimiento
Posición del mouse

mouseX;
mouseY; Sintaxis: (mouseX, mouseY); Ejemplo: ellpise(mouseX, mouseY, 100,100);
esto se puede hacer con cualquier figura, tamaño, posición

Variables integradas
Se puede crear tus propias variables
como se crea varuable personal:

Declarar tu variable
Inicializar tu variable
Usar tu variable Para declrar una variable podemos usar let para variables deinámicas y const para variables constantes. (var se usaba antes para ler) Para poner dos palabras en una se utiliza primero minúscula y luego mayúscula para el cambio de palabras (EJ: circuloMorado) La variable debe ser el primer punto que debe estar escrito. DATO el cuarto número de RGB es la transparencia del color.
Javascript Objects
Para organizar nuestro código de una forma adecuada y ordenada.
Para agrupar muchas variables dentro de una variable.
Es una esctrutura de datos que te permite agrupae valores relacionados bajo un mismo nombre. En lugar de tener muchas variables sueltas, los objetos funcionan como un "conectador" que organiza la info mediante pares de clave y valor.

Luego se accede a la info mediante notación de punto.
(Es igual, solo una manera más ordenada)

Random Fuction
random();
function Su trabajo es devolver un número aleatorio dentro de un rango que tú definas. -> random(): Si no pones nada, devuelve un número decimal entre 0 y 1.
}

